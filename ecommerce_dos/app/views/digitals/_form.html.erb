<%= form_with(model: digital, local: true) do |form| %>
  <% if digital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(digital.errors.count, "error") %> prohibited this digital from being saved:</h2>

      <ul>
      <% digital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>
  
  <div class="field">
    <%= form.label :stock %>
    <%= form.number_field :stock %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
